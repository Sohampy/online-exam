import { useState } from 'react';import { Link, useNavigate } from 'react-router-dom';import { useAuth } from '../../contexts/AuthContext.jsx';
export default function Register(){const [fullName,setFullName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const {register}=useAuth();const nav=useNavigate();async function submit(e){e.preventDefault();const {error}=await register(fullName,email,password);if(error)setMsg(error.message);else{setMsg('Registered successfully. Check email confirmation if enabled, then login.');setTimeout(()=>nav('/login'),1200)}}return <div className="auth-card"><h2>Student Registration</h2><form onSubmit={submit}><input placeholder="Full Name" value={fullName} onChange={e=>setFullName(e.target.value)}/><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn">Create Account</button></form>{msg&&<p>{msg}</p>}<p>Already registered? <Link to="/login">Login</Link></p></div>}
