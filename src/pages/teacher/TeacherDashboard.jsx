import { Link } from 'react-router-dom';import { useAuth } from '../../contexts/AuthContext.jsx';
export default function TeacherDashboard(){const {profile}=useAuth();return <><h1>Teacher Dashboard</h1><div className="cards"><div className="card"><h3>Assigned Exams</h3><p>View exams created by examiner.</p><Link className="btn" to="/teacher/exams">Open</Link></div><div className="card"><h3>Reports</h3><p>{profile?.can_view_reports?'You can view reports.':'Report permission not enabled.'}</p><Link className="btn secondary" to="/teacher/reports">View</Link></div><div className="card"><h3>Questions</h3><p>{profile?.can_manage_questions?'You can manage questions from admin question bank URL if allowed.':'Question permission not enabled.'}</p></div></div></>}
